node_modules
dist
.env
yarn.lock
yarn-error.log

/src/config/sentry.js

# Ignore test-related files
/coverage.data
/coverage/

# Dump gerado pelo redis-server quando rodado localmente (ex.: ambiente de
# desenvolvimento) — não é conteúdo do projeto.
dump.rdb

